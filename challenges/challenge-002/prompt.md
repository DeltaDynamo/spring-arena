# POST /users validation

Implement a REST endpoint `POST /users` that accepts a user object.

Requirements:
- Return `400 Bad Request` when the request body is empty or missing required fields.
- Return `200 OK` when a valid payload is sent.
- Keep validation logic simple and explicit.
