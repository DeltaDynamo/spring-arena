# Build GET /users/{id}

Implement a REST API endpoint `GET /users/{id}`.

Requirements:
- Return `200 OK` for user id `1` with a JSON payload.
- Return `404 Not Found` for any unknown user id.
- Keep the response body clear and aligned with REST semantics.
